dependencies {
    api project(':tale')
    api project(':actors')
    testImplementation 'junit:junit:4.12'
}

mrjar{ 
    releases = [JavaVersion.VERSION_11]
    moduleName = 'org.gradle.fairy.tale.formula'
}